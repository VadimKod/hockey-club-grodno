version: '3.8'
services:
  mongo:
    image: mongo:7
    container_name: hockey_mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: hockey_club
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
